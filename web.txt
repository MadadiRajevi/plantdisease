🚀 STEP–BY–STEP: GitHub Webhook Using Jenkins + Ngrok
✅ STEP 1 — Start Jenkins Locally

Open Jenkins on your system:

http://localhost:8080


Make sure Jenkins is running before starting ngrok.

✅ STEP 2 — Start ngrok on Port 8080

Open a new CMD and run:

ngrok http 8080


ngrok will give you something like:

Forwarding  https://a1b2c3d4.ngrok.io -> http://localhost:8080


Copy the HTTPS URL example:

https://a1b2c3d4.ngrok.io

✅ STEP 3 — Add Webhook in GitHub

Go to your GitHub repo:

Settings → Webhooks → Add Webhook

Fill like this:

Payload URL
https://a1b2c3d4.ngrok.io/github-webhook/


⚠️ Always end with /github-webhook/

Content type

Select:

application/json

Events

Choose:

Just the push event


Click Add Webhook
A green tick (✓) should appear.

✅ STEP 4 — Configure Your Jenkins Job

Open Jenkins → Your Project → Configure

Under Source Code Management → Git

Add repo URL + credentials (if private)

Scroll to:

Build Triggers

Tick:

GitHub hook trigger for GITScm polling


Save the job.

✅ STEP 5 — Test the Webhook

Make a small change in GitHub → push.

GitHub → Webhooks → recent deliveries
You should see:

Response: 200 OK


And Jenkins will automatically start building.

🚫 If It Fails — Troubleshooting Checklist
1️⃣ Check ngrok is still running

Ngrok URL changes every time — if you close the terminal → URL is gone.

2️⃣ Update Webhook URL again

Every time ngrok gives a new URL, update GitHub webhook.

3️⃣ Install Required Plugin

In Jenkins:

Manage Jenkins → Manage Plugins → Available


Install:

GitHub Integration Plugin

4️⃣ Security Group Not Needed

Because everything is happening locally → no EC2 required.